261"}
Apply the following fixes to the Google Apps Script email relay.

Important constraints:
- Do NOT change template names.
- Do NOT change the request format expected by Supabase Edge Functions.
- Do NOT modify email styling or layout.
- Only apply the fixes listed below.

-------------------------------------

FIX 1 — Add HTML escaping helper

At the top of the script (after global constants), add this function:

function esc(s) {
  return String(s || '')
    .replace(/&/g,'&')
    .replace(/</g,'<')
    .replace(/>/g,'>')
    .replace(/"/g,'"');
}

This function will safely escape user-supplied text before inserting it into email HTML.

-------------------------------------

FIX 2 — Escape all user-supplied HTML fields

Wherever user-supplied fields are inserted into HTML, wrap them with esc().

Specifically apply esc() to these payload fields:

- d.message
- d.reason
- d.signature
- d.notes
- d.first_name
- d.last_name
- d.property
- d.property_address
- d.tenant_name
- any other user-entered string values

Example:

Before:
${d.message}

After:
${esc(d.message)}

Do NOT escape URLs or system-generated values.

-------------------------------------

FIX 3 — Prevent tenant_name null crash

Replace both occurrences of:

d.tenant_name.split(' ')[0]

with:

(d.tenant_name || '').split(' ')[0] || 'Tenant'

Locations identified in audit:
- sendLeaseSent
- sendLeaseSentCoApplicant

This prevents runtime crashes if tenant_name is missing.

-------------------------------------

FIX 4 — Remove endpoint information leak

Replace the existing doGet handler with:

function doGet() {
  return ContentService
    .createTextOutput('Not found')
    .setMimeType(ContentService.MimeType.TEXT);
}

This prevents exposing that the relay endpoint exists.

-------------------------------------

Verification after applying changes:

1. Confirm all 18 templates still exist in the dispatch table.
2. Confirm doPost authentication using RELAY_SECRET remains unchanged.
3. Confirm all emails still render correctly.
4. Confirm no syntax errors exist.

Return a summary listing:
- files modified
- number of esc() applications added
- confirmation that the relay behavior is unchanged except for the security f