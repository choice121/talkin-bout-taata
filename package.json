{
  "name": "choice-properties",
  "version": "1.0.0",
  "private": true,
  "description": "Static frontend for Replit — No backend. API handled by Supabase Edge Functions.",
  "scripts": {
    "start": "node server.js",
    "build": "node generate-config.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
